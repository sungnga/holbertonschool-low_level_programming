#include <stdio.h>

/**
 * main - prints the alphabet in lowercase
 * ch is a char type
 * Return: a zero value
 */
int main(void)
{
	int num;

	for (num = '0'; num <= '9'; num++)
	{
		putchar(num);
	}
		putchar('\n');
	return (0);
}
